<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hack.scarlet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="hack.scarlet.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="hack.scarlet.LoginActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name="hack.scarlet.SignupActivity" android:theme="@style/AppTheme.Dark" />
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
